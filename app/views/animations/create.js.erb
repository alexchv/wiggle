<% if @animation.errors.any? %>
    $(".create-animation").replaceWith("<%= j(render({:partial => 'animations/partials/form_modal', :locals => {:animation => @animation} })) %>");
<% else %>
    window.lazyModal.close();
<% end %>