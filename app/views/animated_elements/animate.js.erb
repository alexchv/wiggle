$(document).find('head').append("<link rel='stylesheet' media='all' href='http://wiggle-beta.herokuapp.com/animate.css'>");

if (!window.jQuery) {
    $(document).find('head').append("<script src='http://wiggle-beta.herokuapp.com/jquery.js'></script>");
}

window.onload = function() {
    <% if @user.present? %>

        <% @user.animated_elements.each do |al| %>

            var elem = $(document).find('<%= j("#{al.html_tag}#{al.classes_to_jq}#{al.id_to_jq}") %>');

            if (elem) {
                var $elem = $(elem);

                $elem.css({"-vendor-animation-duration": '<%= j(al.animation.duration.to_s) %>', "-vendor-animation-delay": '<%= j(al.animation.delay.to_s) %>', "-vendor-animation-iteration-count": '<%= j(al.animation.count.to_s) %>'});
                $elem.addClass('<%= j([al.animation_type.name, 'animated'].join(' ')) %>');
            }

        <% end %>
<% end %>
};







